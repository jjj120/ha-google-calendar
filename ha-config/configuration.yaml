# Loads default set of integrations. Do not remove.
default_config:

# Lovelace configuration in YAML mode
lovelace:
  mode: yaml
  resources:
    - url: /google_calendar_card/google-calendar-card.js?v=1.0.1
      type: module
    - url: /local/google-calendar-card.js?v=1.0.1
      type: module

# Google Calendar Card Helper integration (automatically registers card via add_extra_js_url)
google_calendar_card:
